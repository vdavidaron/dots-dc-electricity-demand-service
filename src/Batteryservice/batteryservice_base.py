from datetime import datetime
import helics as h
from dots_infrastructure.DataClasses import TimeStepInformation, EsdlId, HelicsCalculationInformation, PublicationDescription, SubscriptionDescription
from dots_infrastructure.HelicsFederateHelpers import HelicsSimulationExecutor
from esdl import EnergySystem, Battery
from dots_infrastructure.EsdlHelperFunctions import EsdlHelperFunctions

# This file is generated, DO NOT edit this file!

class BatteryserviceBase(HelicsSimulationExecutor):

    def __init__(self):
        super().__init__()
        self.esdl_obj_mapping : dict[EsdlId, Battery]= {}

        
        # Calculation: daily_degradation
        self.daily_degradation_period_seconds = 86400
        daily_degradation_inputs = [
        
        ]
        daily_degradation_outputs = [
        
            PublicationDescription(global_flag=True, 
                                    esdl_type="Battery",
                                    output_name="health_capacity_degradation",
                                    output_unit="pct", 
                                    data_type=h.HelicsDataType.DOUBLE),
        ]
        daily_degradation_information = HelicsCalculationInformation(
            time_period_in_seconds=86400,
            offset=0, 
            uninterruptible=False, 
            wait_for_current_time_update=False, 
            terminate_on_error=True, 
            calculation_name="daily_degradation", 
            inputs=daily_degradation_inputs, 
            outputs=daily_degradation_outputs, 
            calculation_function=self.daily_degradation
        )
        self.add_calculation(daily_degradation_information)
        # Calculation: battery_state
        self.battery_state_period_seconds = 900
        battery_state_inputs = []
        battery_state_outputs = [
            PublicationDescription(global_flag=True, 
                                    esdl_type="Battery",
                                    output_name="bess_power_w",
                                    output_unit="W", 
                                    data_type=h.HelicsDataType.DOUBLE),
            PublicationDescription(global_flag=True, 
                                    esdl_type="Battery",
                                    output_name="state_of_charge",
                                    output_unit="pct", 
                                    data_type=h.HelicsDataType.DOUBLE),
            PublicationDescription(global_flag=True, 
                                    esdl_type="Battery",
                                    output_name="max_available_charge",
                                    output_unit="W", 
                                    data_type=h.HelicsDataType.DOUBLE),
            PublicationDescription(global_flag=True, 
                                    esdl_type="Battery",
                                    output_name="max_available_discharge",
                                    output_unit="W", 
                                    data_type=h.HelicsDataType.DOUBLE),
        ]
        battery_state_information = HelicsCalculationInformation(
            time_period_in_seconds=900,
            offset=0, 
            uninterruptible=False, 
            wait_for_current_time_update=False, 
            terminate_on_error=True, 
            calculation_name="battery_state", 
            inputs=battery_state_inputs, 
            outputs=battery_state_outputs, 
            calculation_function=self.battery_state
        )
        self.add_calculation(battery_state_information)

        # Calculation: battery_dispatch
        self.battery_dispatch_period_seconds = 900
        battery_dispatch_inputs = [
            SubscriptionDescription(esdl_type="ElectricityNetwork", input_name="bess_allocation_w", input_unit="W", input_type=h.HelicsDataType.DOUBLE),
        ]
        battery_dispatch_outputs = []
        battery_dispatch_information = HelicsCalculationInformation(
            time_period_in_seconds=900,
            offset=0, 
            uninterruptible=False, 
            wait_for_current_time_update=False, 
            terminate_on_error=True, 
            calculation_name="battery_dispatch", 
            inputs=battery_dispatch_inputs, 
            outputs=battery_dispatch_outputs, 
            calculation_function=self.battery_dispatch
        )
        self.add_calculation(battery_dispatch_information)

    def init_calculation_service(self, energy_system: EnergySystem):
        all_esdl_objs = EsdlHelperFunctions.get_all_esdl_objects_from_type(energy_system.eAllContents(), Battery)
        for esdl_obj in all_esdl_objs:
            if hasattr(esdl_obj, "id"):
                self.esdl_obj_mapping[esdl_obj.id] = esdl_obj

    
    def daily_degradation(self, param_dict : dict, simulation_time : datetime, time_step_number : TimeStepInformation, esdl_id : EsdlId, energy_system : EnergySystem):
        pass
    
    def battery_state(self, param_dict : dict, simulation_time : datetime, time_step_number : TimeStepInformation, esdl_id : EsdlId, energy_system : EnergySystem):
        pass

    def battery_dispatch(self, param_dict : dict, simulation_time : datetime, time_step_number : TimeStepInformation, esdl_id : EsdlId, energy_system : EnergySystem):
        pass